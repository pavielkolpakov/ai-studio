# CRUD Module

- `crud_conversations.py` — `get_or_create_conversation()`, `get_conversation()` (query only), `append_message()` (appends to JSONB array), `get_recent_messages()` (last 10)
- `crud_contact.py` — `create_contact_submission()` (stores contact form data + optional transcript)
